本例程分别演示
	高速内部内部振荡器时钟：fIH 作为CPU运行时钟以及外围电路的时钟
	外部高速系统时钟：fMX   作为CPU运行时钟以及外围电路的时钟         <-- 需要板子上接无源晶振（最大20MHz）用 fMX作为 串口的运行时钟
	低速内部振荡器时钟：fIL 作为CPU运行时钟以及外围电路的时钟 ；具体可以通过TIMER 40 的方波输出，P01引脚输出方波，观察系统运行时钟
	外部低速系统时钟：fSUB 作为CPU运行时钟以及外围电路的时钟 ；具体可以通过TIMER 40 的方波输出，P01引脚输出方波，观察系统运行时钟
	同时设置外部低速系统时钟、和外部高速系统时钟：fMX 作为CPU运行时钟以及外围电路的时钟 ；fSUB通过P15输出32.768khz 时钟

